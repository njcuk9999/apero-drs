Can you add a secure forgot-password and reset-password flow with token expiry, generic responses (no account enumeration), and basic rate limiting?

========================================

Can you tighten registration usernames so they are lowercase letters/numbers/underscore only, and update the validation message accordingly?

========================================

On the home page welcome text, show the user's first name instead of username.

========================================

Can you fix async task output_files duplication so repeated runs do not accumulate duplicate entries?

========================================

Please implement first-run setup and startup guard behavior: add apero_ri_setup, setup state/bootstrap helpers, and prevent apero_ri_run from starting until setup is completed.

========================================

Can you make ARI_DIR/data-dir handling consistent across app startup and auth/user-data paths so setup/run use the same local data directory?