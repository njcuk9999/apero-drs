"""apero_ri.components - shared UI section descriptors.

Contains "section" definitions that are rendered in multiple pages
(e.g. the astrometrics resolve-target tab and the data-portal object
page target_info tab).  Adding a new section here updates BOTH pages.
"""
