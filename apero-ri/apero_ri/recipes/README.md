# recipe directory

Top level "recipes" - scripts that are run - no "real code" should be here
just top level instructions to run. These are the codes run by the user.

Probably this will just be a "setup" and a "run" script