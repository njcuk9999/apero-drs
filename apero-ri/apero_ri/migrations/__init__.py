"""One-off migration / back-fill scripts for the APERO RI.

Each module in this package is a standalone script that is run
manually (typically once) to migrate data from one schema / format
to another. They are *not* invoked by the running web app.
"""
