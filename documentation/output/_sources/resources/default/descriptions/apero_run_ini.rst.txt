The apero_run_ini re-generates all :term:`run-ini-files` for all instruments using the
current file_definitions and recipe_definitions for each instrument.

This does not update the users run.ini files only the ones within the package.
Run apero_reset to update user run.ini files.