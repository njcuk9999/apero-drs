# Note this is a trigger file
#   Please copy this before making changes
# FOR USE WITH VERSION 0.6.129 (2020-07-29)

# -----------------------------------------------------------------------------
# Core variables
# -----------------------------------------------------------------------------
# Define run name
RUN_NAME = "Trigger Night Calib Run"

# Define whether to send email (required yagmal)
#   to install yagmail: pip install yagmail
# Note this currently is untested
SEND_EMAIL = False

# Define email address to send/recieve (send to self)
# Note this currently is untested
EMAIL_ADDRESS = None

# Define night name (for multiple set to None)
NIGHTNAME = None

# Define black list of night names (None means no filter)
#    BNIGHTNAMES = 2018-07-31, 2018-08-05, 2018-12-18, 2018-12-19, 2019-01-17
BNIGHTNAMES = None
# Define white list of night names (None means no filter)
#    WNIGHTNAMES = 2018-07-31, 2018-08-01, 2018-08-02, 2018-08-03
WNIGHTNAMES = None

# Define a pi name list for filtering by (None means no filter)
PI_NAMES = None

# define master night
MASTER_NIGHT = 2019-04-20

# Number of cores
CORES = 5

# Stop on exception
STOP_AT_EXCEPTION = False

# Run in test mode (does not run codes)
TEST_RUN = True

# Whether to process engineering data
ENGINEERING = False

# Whether this is a trigger run
TRIGGER_RUN = True

# Use odometer reject list
USE_ODO_REJECTLIST = True

# Recalculate templates if template already exists
RECAL_TEMPLATES = False

# -----------------------------------------------------------------------------
# Define what to reset

# !!!!!!!!!!!!!! WARNING !!!!!!!!!!!!!!!!!
#     These will delete all files
# !!!!!!!!!!!!!! WARNING !!!!!!!!!!!!!!!!!
# -----------------------------------------------------------------------------
# Switch to allow prompt resets (should be set to False unless sure)
RESET_ALLOWED = False
# If True will prompt to reset the DRS_DATA_WORKING (working) directory
RESET_TMP = True
# If True will prompt to reset the DRS_DATA_REDUC (reduced) directory
RESET_REDUCED = True
# If True will prompt to reset the DRS_CALIB_DB (calibDB) directory
RESET_CALIB = True
# If True will prompt to reset the DRS_TELLU_DB (telluDB) directory
RESET_TELLU = True
# If True will prompt to reset the DRS_DATA_MSG (log) directory
RESET_LOG = True
# If True will prompt to reset the DRS_DATA_PLOT (plot) directory
RESET_PLOT = True
# If True will prompt to reset the DRS_DATA_RUN (run) directory
RESET_RUN = True

# -----------------------------------------------------------------------------
# Define what to run
# -----------------------------------------------------------------------------
# Run the preprocessing recipes
RUN_PP = False
RUN_PP_CAL = False
RUN_PP_SCI = True
RUN_PP_TEL = False

# Run the dark recipes
RUN_DARKM = False

# Run the bad pixel recipes
RUN_BADM = False
RUN_BAD = False

# Run the localisation recipes
RUN_LOC = False
RUN_LOCM = False

# Run the shape recipes
RUN_SHAPEM = False
RUN_SHAPE = False
RUN_SHAPELM = False

# Run the flat fielding recipes
RUN_FF = False
RUN_FLATM = False

# Run the thermal correction recipes
RUN_THERM = False
RUN_THIM = False
RUN_THTM = False

# Run the wave length recipes
RUN_WAVE = False
RUN_WAVEM = False

# Run the extraction recipes
RUN_EXT = True
RUN_EXTTELL = True
RUN_EXTOBJ = True
RUN_EXTALL = True

# Run the leak correction recipes
RUN_LEAKM = True
RUN_LEAKTELL = True
RUN_LEAKOBJ = True
RUN_LEAKALL = True

# Run the make telluric recipes
RUN_MKTELLU1 = True
RUN_MKTELLU2 = True
RUN_MKTELLU3 = True
RUN_MKTELLU4 =  True

# Run the fit telluric recipes
RUN_FTELLU1 = True
RUN_FTELLU2 = True
RUN_FTELLU3 = True

# Run the make_tellu/fit_tellu database scripts
#    Note: not used in sequence "limited_run"
RUN_MKTELLDB = False
RUN_FTELLDB = False

# Run the CCF
RUN_CCF = True

# -----------------------------------------------------------------------------
# Define what to skip (if files found)
# -----------------------------------------------------------------------------
# Skip preprocessing recipes if file(s) exist
SKIP_PP = True
SKIP_PP_CAL = True
SKIP_PP_SCI = True
SKIP_PP_TEL = True

# Skip dark recipes if file(s) exist
SKIP_DARKM = True

# Skip bad pixel recipes if file(s) exist
SKIP_BADM = True
SKIP_BAD = True

# Skip localisation recipes if file(s) exist
SKIP_LOC = True
SKIP_LOCM = True

# Skip shape recipes if file(s) exist
SKIP_SHAPEM = True
SKIP_SHAPE = True
SKIP_SHAPELM = True

# Skip flat fielding recipes if file(s) exist
SKIP_FF = True
SKIP_FLATM = True

# Skip thermal correction recipes if file(s) exist
SKIP_THERM = True
SKIP_THIM = True
SKIP_THTM = True

# Skip wave length recipes if file(s) exist
SKIP_WAVE = True
SKIP_WAVEM = True

# Skip extraction recipes if file(s) exist
SKIP_EXT = True
SKIP_EXTTELL = True
SKIP_EXTOBJ = True
SKIP_EXTALL = True

# Skip make telluric recipes if file(s) exist
SKIP_MKTELLU1 = True
SKIP_MKTELLU2 = True
SKIP_MKTELLU3 = True
SKIP_MKTELLU4 =  True

# Skip fit telluric recipes if file(s) exist
SKIP_FTELLU1 = True
SKIP_FTELLU2 = True
SKIP_FTELLU3 = True

# Skip CCF recipes
SKIP_CCF = True

# -----------------------------------------------------------------------------
# Run information
# -----------------------------------------------------------------------------
#
#   Format:
#
#   id##### = command
#
#   Must start with "id" followed by a number
#     The number identifies the order to process in
#
#   If command is a sequence will process all in defined sequence
#      valid sequences are:
#           pp_seq, pp_seq_opt, full_seq,
#           limited_seq, master_seq, calib_seq,
#           tellu_seq, science_seq, eng_seq

# pre-processing
id00000 = pp_seq_opt
id00001 = science_seq





